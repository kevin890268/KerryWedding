.DS_Store
Thumbs.db
*.log

# screen recordings: too heavy for the repository
*.MP4
*.mp4
*.mov

# the full-resolution originals (about 300 MB); the page uses the web copies in assets/photos
pic/

# font downloads (the page loads the same fonts from Google Fonts)
*.zip
